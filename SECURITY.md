# 安全说明

## 密钥

- 只通过 `DEEPSEEK_API_KEY` 环境变量或本机 `.env` 注入密钥。
- `.env` 已被 Git 忽略；不要把真实密钥写进代码、日志、Issue 或聊天记录。
- OpenClaw 部署建议把密钥放在权限为 `600` 的服务环境文件中。
- 本项目不会要求证券账户、交易密码或支付凭据。

如果密钥曾被提交到 Git，请立即在 DeepSeek 控制台撤销并重新生成；仅删除文件不能清除 Git 历史。

## 产品边界

本项目不连接证券账户，不自动交易，不承诺收益。历史情景概率不是未来结果保证。

内容仅供研究参考，不构成投资建议。
